import { useEffect } from "react"

fucn p ()
const pro , setPro = use([])
CONST SLECTiD, SETI = SUEsTA(n)

useEffect(() => {
    const stp = JSON.parse(localSgt'o)
        if(strod{
            setPro(setPro
                fetch('/prodc.json')
                .then(res => rmSync.json9)
                .daa => {
                    se9da
                }
            )
        })
    }, [])